import React from 'react';

interface ArtistListProps {
  keyword: string;
}

const ArtistList: React.FC<ArtistListProps> = ({ keyword }) => {
  return <div>ArtistList</div>;
};

export default ArtistList;
